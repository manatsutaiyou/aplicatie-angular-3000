/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TipuriDeReteleService } from './tipuri-de-retele.service';

describe('Service: TipuriDeRetele', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TipuriDeReteleService]
    });
  });

  it('should ...', inject([TipuriDeReteleService], (service: TipuriDeReteleService) => {
    expect(service).toBeTruthy();
  }));
});
